/**JavaScript slice method extracts a part of the string based on the 
   given stating-index and ending-index and returns a new string. */

let str = "This is javascript slice method.";
let b = str.slice(0, 4);
let c = str.slice(5, 7);
console.log(b);
console.log(c);
