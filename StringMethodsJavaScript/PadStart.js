/*JavaScript padStart() method pad a string with another string until it reaches the given length. 
The padding is applied from the left end of the string. */

let stone = "Soul";
// stone = stone.padStart(6, "Mind ");
// stone = stone.padStart(7, "Mind ");
// stone = stone.padStart(8, "Mind ");
stone = stone.padStart(9, "Mind ");
//stone = stone.padStart(10, "Mind ");
console.log(stone);
