/*JavaScript replace() method replaces a part of the given string with another string or a regular expression. 
  The original string will remain unchanged. Case sensitive 
 */

let str = "Mind, Power, Soul";

let newStr = str.replace("Power", "Space");
console.log(newStr);
