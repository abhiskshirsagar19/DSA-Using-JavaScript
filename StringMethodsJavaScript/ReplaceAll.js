/*JavaScript replaceAll() method returns a new string after replacing all the matches of a string with a specified string or a regular expression. 
The original string is left unchanged after this operation. */
let str = "Mind, Power, Soul, Power, Space, Space";

let newStr = str.replaceAll("Power", "Space");
console.log(newStr);
